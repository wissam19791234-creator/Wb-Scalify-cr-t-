import {useState} from 'react';import {Link} from 'react-router-dom';import {SectionHeader} from '../components/ui/SectionHeader';import {GlassCard} from '../components/ui/GlassCard';import {LiquidButton} from '../components/ui/LiquidButton';import {portfolio,sectors} from '../data/portfolio';import {scrollToSection} from '../utils/scrollToSection';
export const Portfolio=()=>{const [f,setF]=useState('Tous');const items=f==='Tous'?portfolio:portfolio.filter(p=>p.sector===f);return <section id="realisations" className="section-soft px-4 py-24"><div className="mx-auto max-w-7xl"><SectionHeader title="Des réalisations pensées pour vendre." subtitle="Cliquez sur un projet pour ouvrir une vraie démo."/><div className="mb-8 flex flex-wrap justify-center gap-2">{sectors.map(s=><LiquidButton key={s} variant={f===s?'primary':'ghost'} onClick={()=>setF(s)}>{s}</LiquidButton>)}</div><div className="grid gap-6">{items.map((p,i)=><GlassCard className={`grid items-center gap-6 md:grid-cols-2 ${i%2?'md:[&>div:first-child]:order-2':''}`} key={p.title}><div className="demo-photo min-h-72 rounded-3xl p-6"><b className="text-3xl">{p.title}</b><p className="mt-20 text-soft">{p.sector} · {p.visualType}</p></div><div><span className="text-cyan">{p.sector}</span><h3 className="mt-2 text-3xl font-black">{p.title}</h3><p className="mt-3 text-soft">{p.description}</p><div className="mt-5 flex flex-wrap gap-2">{p.features.map(x=><span className="rounded-full bg-white/7 px-3 py-1 text-sm" key={x}>{x}</span>)}</div><div className="mt-6 flex flex-wrap gap-3"><Link to={p.url}><LiquidButton>Voir la démo</LiquidButton></Link><LiquidButton variant="ghost" onClick={()=>scrollToSection('#contact',{projectType:'Démo gratuite',activity:p.sector,message:`Je veux une démo similaire à ${p.title}.`})}>Je veux une démo similaire</LiquidButton></div></div></GlassCard>)}</div></div></section>}

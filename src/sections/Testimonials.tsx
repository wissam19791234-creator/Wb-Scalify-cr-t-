import {useState} from 'react';import {SectionHeader} from '../components/ui/SectionHeader';import {GlassCard} from '../components/ui/GlassCard';import {LiquidButton} from '../components/ui/LiquidButton';import {testimonials} from '../data/testimonials';
export const Testimonials=()=>{const [more,set]=useState(false);return <section id="avis" className="section-dark px-4 py-24"><div className="mx-auto max-w-7xl"><SectionHeader title="Ils ont compris ce qu’un bon site peut changer." subtitle="Exemples de retours clients — à remplacer par vos vrais témoignages."/><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{testimonials.slice(0,more?6:4).map(t=><GlassCard key={t[0]}><div className="text-cyan">★★★★★</div><p className="mt-4 text-soft">“{t[2]}”</p><b className="mt-5 block">{t[0]} — {t[1]}</b></GlassCard>)}</div><div className="mt-8 text-center"><LiquidButton onClick={()=>set(!more)}>{more?'Réduire les avis':'Voir plus d’avis'}</LiquidButton></div></div></section>}

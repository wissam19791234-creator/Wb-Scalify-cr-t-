import {useState} from 'react';import {SectionHeader} from '../components/ui/SectionHeader';import {GlassCard} from '../components/ui/GlassCard';import {automationOptions} from '../data/automationOptions';
export const AIAutomations=()=>{const [a,setA]=useState(0);const d=automationOptions[a];return <section id="automatisations" className="section-dark px-4 py-24"><div className="mx-auto max-w-7xl"><SectionHeader title="L’IA utile, pas l’IA gadget." subtitle="L’IA prépare, trie et structure. Vous gardez la main."/><div className="grid gap-6 lg:grid-cols-[1fr_.8fr]"><div className="grid gap-3 sm:grid-cols-2">{automationOptions.map((o,i)=><button className={`card-glass rounded-2xl p-4 text-left ${a===i?'border-cyan/60':''}`} onClick={()=>setA(i)} key={o.name}>{o.name}</button>)}</div><GlassCard><h3 className="text-2xl font-black">{d.name}</h3><p className="mt-4 text-soft">{d.example}</p><p className="mt-4 text-success">Bénéfice : {d.benefit}</p><p className="mt-4 text-cyan">Outils : {d.tools}</p></GlassCard></div></div></section>}

import {useState} from 'react';import {SectionHeader} from '../components/ui/SectionHeader';import {GlassCard} from '../components/ui/GlassCard';import {LiquidButton} from '../components/ui/LiquidButton';import {scrollToSection} from '../utils/scrollToSection';
export const Solution=()=>{const [withS,set]=useState(true);const list=withS?['site clair','formulaire intelligent','chatbot 24h/24','dashboard','relances possibles','traduction multilingue','demandes mieux structurées']:['infos dispersées','DM flous','appels manqués','aucune relance','image moins pro','aucun système clair'];return <section id="solution" className="section-dark px-4 py-24"><div className="mx-auto max-w-5xl"><SectionHeader title="Scalify transforme l’attention en demande claire." subtitle="On structure votre site pour que le visiteur comprenne vite, choisisse facilement et vous contacte avec les bonnes informations."/><GlassCard><div className="flex gap-3"><LiquidButton variant={!withS?'primary':'ghost'} onClick={()=>set(false)}>Sans Scalify</LiquidButton><LiquidButton variant={withS?'primary':'ghost'} onClick={()=>set(true)}>Avec Scalify</LiquidButton></div><div className="mt-8 grid gap-3 md:grid-cols-2">{list.map(x=><div className="rounded-2xl bg-white/7 p-4" key={x}>{withS?'✓':'×'} {x}</div>)}</div><LiquidButton className="mt-8" onClick={()=>scrollToSection('#contact',{projectType:'Démo gratuite'})}>Recevoir ma démo gratuite</LiquidButton></GlassCard></div></section>}

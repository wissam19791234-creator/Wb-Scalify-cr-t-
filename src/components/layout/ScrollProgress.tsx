import {useEffect,useState} from 'react';export const ScrollProgress=()=>{const [w,setW]=useState(0);useEffect(()=>{const f=()=>setW(scrollY/(document.body.scrollHeight-innerHeight)*100);addEventListener('scroll',f,{passive:true});f();return()=>removeEventListener('scroll',f)},[]);return <div className="fixed left-0 top-0 z-[70] h-1 bg-gradient-to-r from-cyan to-violet" style={{width:`${w}%`}}/>}

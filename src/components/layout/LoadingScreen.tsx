import {useEffect,useState} from 'react';export const LoadingScreen=()=>{const [p,setP]=useState(0);useEffect(()=>{const t=setInterval(()=>setP(v=>Math.min(100,v+5)),80);return()=>clearInterval(t)},[]);return <div className={`fixed inset-0 z-[80] grid place-items-center bg-black transition duration-700 ${p>=100?'pointer-events-none opacity-0':''}`}><div className="text-center"><div className="mb-6 text-4xl font-black tracking-[.35em]">SCALIFY<span className="gradient-text">.</span></div><div className="h-2 w-72 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-gradient-to-r from-cyan to-violet" style={{width:`${p}%`}}/></div><p className="mt-4 text-sm tracking-[.3em] text-soft">DESIGNING DIGITAL EXPERIENCES</p><b className="mt-2 block text-cyan">{p}%</b></div></div>}

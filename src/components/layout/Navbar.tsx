import {Menu,X} from 'lucide-react';import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {scrollToSection} from '../../utils/scrollToSection';import {LiquidButton} from '../ui/LiquidButton';
const links=[['Accueil','#accueil'],['Problème','#probleme'],['Parcours','#parcours'],['Réalisations','#realisations'],['Avis','#avis'],['Offres','#offres'],['Contact','#contact']];
export const Navbar=()=>{const [open,setOpen]=useState(false);const nav=useNavigate();const loc=useLocation();const go=(id:string)=>{setOpen(false); if(loc.pathname!=='/'){nav('/'); setTimeout(()=>scrollToSection(id),50)} else scrollToSection(id)};return <nav className="sticky top-0 z-50 border-b border-white/10 bg-black/65 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link to="/" className="font-black tracking-[.25em]">SCALIFY<span className="gradient-text">.</span></Link><div className="hidden items-center gap-5 lg:flex">{links.map(([l,id])=><button className="text-sm text-soft hover:text-white" onClick={()=>go(id)} key={id}>{l}</button>)}<LiquidButton variant="ghost" onClick={()=>go('#dashboard')}>Espace Client</LiquidButton><LiquidButton onClick={()=>go('#contact')}>Demander un devis</LiquidButton></div><button className="lg:hidden" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X/>:<Menu/>}</button></div>{open&&<div className="grid gap-3 border-t border-white/10 p-4 lg:hidden">{links.map(([l,id])=><button className="text-left text-soft" onClick={()=>go(id)} key={id}>{l}</button>)}</div>}</nav>}

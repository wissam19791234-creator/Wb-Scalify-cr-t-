import {useState} from 'react';const steps=['Visiteur intéressé','Message flou','Formulaire intelligent','Demande structurée','Dashboard Scalify','Relance / Devis'];
export const ThreeFlowDiagram=()=>{const [active,setActive]=useState(1);return <div className="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{steps.map((s,i)=><button key={s} onClick={()=>setActive(i)} className={`card-glass rounded-3xl p-5 text-left transition hover:-translate-y-1 ${active===i?'border-cyan/60 shadow-[0_0_40px_rgba(77,226,255,.16)]':''}`}><span className="gradient-text text-2xl font-black">0{i+1}</span><p className="mt-3 font-bold">{s}</p><div className="mt-4 h-1 rounded bg-gradient-to-r from-cyan to-violet"/></button>)}</div><div className="card-glass rounded-3xl p-7"><h3 className="text-2xl font-black">{active===1?'Le problème commence ici.':steps[active]}</h3><p className="mt-4 text-soft">{active===1?'Le client écrit vite, sans donner les informations nécessaires.':'Chaque étape transforme l’attention en information claire et exploitable.'}</p><p className="mt-5 rounded-2xl border border-cyan/20 bg-cyan/10 p-4 text-cyan">Bénéfice : Scalify guide la demande vers une action plus claire.</p></div></div>}
